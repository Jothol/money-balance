'use client';

export default function TopBar() {
  return (
    <header className="w-full h-1/10 border-b border-gray-300 bg-white flex items-center justify-center text-lg font-semibold">
      Expense Tracker
    </header>
  );
}